/* eslint-disable @typescript-eslint/no-var-requires */
// const jwt = require('jsonwebtoken');

// export async function verifyToken(token: any) {
//     return await jwt.verify(token, process.env.JWT_SECRET_KEY);
// }

// export async function generateToken(payload: any, expiresIn = process.env.EXPIRES_IN) {
//     return await jwt.sign(payload, process.env.JWT_SECRET_KEY, { expiresIn: expiresIn });
// }
